(
    function () {
        'use strict';

        angular
            .module('faqjs')
            .controller('faqCtrl',control);

        control.$inject = [
            '$state'
        ];

        function control(
            $state
        ) {
            var vm = angular.extend(this, {

            });

            
        }
       
    }
)();
